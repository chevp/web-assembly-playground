{
  "name": "poc-10-json-mock-backend",
  "version": "0.2.0",
  "private": true,
  "description": "Mock backend for the Cryolite project API. Graph is the source of truth (nodes + edges + components); files exist only as an export/import projection.",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
